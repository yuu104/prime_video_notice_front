import { NextPage } from "next";
import { RegisterContainer } from "src/components/pages/Register";

const RegisterPage: NextPage = () => {
  return <RegisterContainer />;
};

export default RegisterPage;
